git log --all --decorate --oneline --graph

/api/v1/nodes/<node-name>/proxy/<kubelet-path>

Examples of <kubelet-path> (non-exec) are typically things like health/metrics/pod-inventory endpoints. (Those are exactly what monitoring agents scrape.)/api/v1/nodes/<node-name>/proxy/<kubelet-path>

Examples of <kubelet-path> (non-exec) are typically things like health/metrics/pod-inventory endpoints. (Those are exactly what monitoring agents scrape.)